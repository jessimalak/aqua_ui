import 'package:aqua_ui/src/styles/colors.dart';
import 'package:flutter/widgets.dart';

class ColorScheme {
  final Color primary;
  final Color textColor;
  
  const ColorScheme({this.primary = AquaColors.blue, this.textColor = AquaColors.black});
}